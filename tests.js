var TESTRUNNER = {};
//TESTRUNNER.data = { 
 // fonts: ["from-cdn", "local", "local-base64"],
  //resources: ["few", "many"],
//  baseline: ["hello_world"],
 // embedded_images: ["many_files", "single_base64"]
//};

TESTRUNNER.data = {
  baseline: ["hello_world"]
}
